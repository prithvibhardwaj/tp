ignore:
  - "src/main/java/seedu/address/ui"

coverage:
  status:
    project:
      default:
        target: 75%
        threshold: 0%
    patch:
      default:
        target: 75%
        threshold: 0%
